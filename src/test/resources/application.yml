server:
  servlet:
    context-path:

spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

jwt:
  secret: testSecretKeyForTestingOnlyMustBeAtLeast256BitsLong!!
  expiration: 43200000

ai:
  server:
    base-url: http://localhost:9999
    connect-timeout: 1000
    read-timeout: 5000
    context-window: 128000
    api-key: ""
    cf-access-client-id: ""
    cf-access-client-secret: ""
